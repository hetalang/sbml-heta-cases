

t @TimeScale { };

c @Compartment { } .= 2;

S1 @Species { compartment: c, isAmount: true, } .= 1.2;

S1_proc @Process { actors:  = S1, };
S1_proc := 1;
c_proc @Process { actors:  = c, };
c_proc := 1;

