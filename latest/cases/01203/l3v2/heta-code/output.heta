

t @TimeScale { };

y_proc @Process { actors:  = y, };
y_proc := (-2);
x_proc @Process { actors:  = x, };
x_proc := 1;

z @Record { };
z := ((x <= 0.49) ? ((y > 1.49) ? 2 : 1) : ((y > 0.49) ? 1 : 0));
y @Record { } .= 2;
x @Record { } .= 0;

