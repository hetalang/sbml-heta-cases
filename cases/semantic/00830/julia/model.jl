#= 
    This code was generated by heta-compiler of v0.5.8
    
=#

module julia 
using SimSolver

### create default constants
constants_ = NamedTuple{(
  :kf__reaction1_local,:kr__reaction1_local,
)}(Float64[
  0.8,0.06,
])

### initialization of ODE variables and Records
function start_(cons)
    #(kf__reaction1_local,kr__reaction1_local,) = cons

    # Heta initialize
    t = 0.0 # initial time
    C = 1.0
    S1 = 1.0 / C
    S2 = 0.0 / C
    reaction1 = C * (cons[1] * S1 + (-1.0) * cons[2] * S2)
    kf = 0.9
    S3 = 1.5 / C
    kr = 0.075
    S4 = 0.5 / C
    reaction2 = C * (kf * S3 + (-1.0) * kr * S4)
    
    # save results

    return (
        [
            S1 * C,
            S2 * C,
            S3 * C,
            S4 * C,
        ],
        
        [
            C,
            kf,
            kr,
        ]
    )
end

### calculate RHS of ODE
function ode_(du, u, p, t)
    cons = p.constants
    (C,kf,kr,) = p.static
    (S1_,S2_,S3_,S4_,) = u 

    # Heta rules
    S1 = S1_ / C
    S2 = S2_ / C
    reaction1 = C * (cons[1] * S1 + (-1.0) * cons[2] * S2)
    S3 = S3_ / C
    S4 = S4_ / C
    reaction2 = C * (kf * S3 + (-1.0) * kr * S4)
    
    #p.static .= [C,kf,kr,]
    du .= [
      -reaction1,  # dS1_/dt
      reaction1,  # dS2_/dt
      -reaction2,  # dS3_/dt
      reaction2,  # dS4_/dt
    ]
end

### output function
function make_saving_(outputIds::Vector{Symbol})
    function saving_(u, t, integrator)
        cons = integrator.p.constants
        (C,kf,kr,) = integrator.p.static
        (S1_,S2_,S3_,S4_,) = u

        # Heta rules
        S1 = S1_ / C
        S2 = S2_ / C
        reaction1 = C * (cons[1] * S1 + (-1.0) * cons[2] * S2)
        S3 = S3_ / C
        S4 = S4_ / C
        reaction2 = C * (kf * S3 + (-1.0) * kr * S4)
        
        # calculate amounts
        C_ = C
        reaction1_ = reaction1
        reaction2_ = reaction2
        kf_ = kf
        kr_ = kr

        d = Base.@locals
        return [d[id] for id in outputIds]
    end
end

### time events

### discrete events


### continous events


### event assignments


### OUTPUT ###

x00830 = Model(
  start_,
  ode_,
  [
  ],
  make_saving_,
  constants_
)

models = (
    x00830 = x00830
)
tasks = ()

export models, tasks

end
